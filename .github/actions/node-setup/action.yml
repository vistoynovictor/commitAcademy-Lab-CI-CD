name: 'Setup Node.js environment'
description: 'Install Node 22 and restore dependencies'
runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: '22'
        cache: npm
    - run: npm ci
      shell: bash
